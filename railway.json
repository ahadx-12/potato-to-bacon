{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn potatobacon.api.app:app --host 0.0.0.0 --port 8000",
    "healthcheckPath": "/v1/health",
    "healthcheckTimeout": 100
  }
}
