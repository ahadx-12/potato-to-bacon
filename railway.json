{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'uvicorn potatobacon.api.app:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "healthcheckPath": "/v1/health",
    "healthcheckTimeout": 100
  }
}
